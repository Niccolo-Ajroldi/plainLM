
# Define variables
executable=/home/najroldi/plainLM/cluster/eval/single_gpu/condor.sh

# Job specific vars
config=config/14M/eval_ewa.yaml
n_jobs=3

# Args
arguments = $(config) $(Process) $(Cluster)

# Logs
LOGS_DIR=/fast/najroldi/logs/wa_lm
error = $(LOGS_DIR)/err/job.$(Cluster).$(Process).err
output = $(LOGS_DIR)/out/job.$(Cluster).$(Process).out
log = $(LOGS_DIR)/log/job.$(Cluster).$(Process).log

# Specs
request_memory = 500000
request_cpus = 4
request_gpus = 1
requirements = (TARGET.CUDADeviceName == "NVIDIA A100-SXM4-80GB") && (TARGET.Machine != "g125.internal.cluster.is.localnet") && (TARGET.Machine != "g145.internal.cluster.is.localnet")
# requirements = (TARGET.CUDACapability >= 9.0)
# requirements =  (TARGET.CUDACapability >= 8.0) && (TARGET.CUDADeviceName != "NVIDIA A100-SXM4-40GB")

queue $(n_jobs)
